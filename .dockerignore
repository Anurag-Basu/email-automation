node_modules
.next
.git
.data
*.md

# Keep `.env` out of the image build context (avoids secrets in layer cache). For local Docker,
# variables still load via `env_file` + bind mount in docker-compose.yml.
# TODO(before production deploy): keep this; never COPY .env in the Dockerfile.
.env
.env.*
# Never bake GCP keys into the image; compose bind-mounts ./secrets at runtime only.
# secrets/

